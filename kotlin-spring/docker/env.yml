version: "3"
services:
  postgres_main:
    image: postgres:10.4-alpine
    restart: always
    environment:
      POSTGRES_DB: local
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5433:5432"
    volumes:
      - postgres_main_data:/var/lib/postgresql/data
    networks:
      - backend

volumes:
  postgres_main_data: {}

networks:
  backend: